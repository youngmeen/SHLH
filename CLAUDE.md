@AGENTS.md

## Claude Code 전용

위 `AGENTS.md`가 이 저장소의 지시다. 아래는 Claude Code에서만 의미가 있는 내용이다.

- 스크린샷은 `playwright` 플러그인으로 찍는다. 이 프로젝트가 쓰는 플러그인은 `.claude/settings.json`에 있으므로 clone 후 별도 설정이 필요 없다.
- 규칙을 고칠 때는 `AGENTS.md`를 고친다. 이 파일에는 Claude 전용 내용만 둔다.
